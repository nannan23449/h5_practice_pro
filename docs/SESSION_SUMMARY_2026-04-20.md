# 本次对话总结

记录日期：2026-04-20

## 目标

围绕一个“移动端 H5 + Spring Boot 后端”的练手项目，从技术选型、脚手架搭建、本地运行到 GitHub 推送，完成了一轮从 0 到 1 的基础建设。

## 主要完成内容

### 1. 技术选型

- 明确项目不是微信小程序，而是“移动端 Web / H5”
- 确定前后端分离方案
- 前端选型为 `Vue 3 + Vite + TypeScript + Vant`
- 后端选型为 `Spring Boot 3 + Spring Security + MyBatis-Plus`

### 2. 脚手架搭建

- 创建前端项目 `h5-web`
- 创建后端项目 `server-api`
- 补齐基础目录结构
- 增加登录页、首页、接口封装、统一返回结构、异常处理

### 3. 环境准备

- 检查并确认本机已有 `node`、`npm`
- 安装 `JDK`
- 安装 `Maven`
- 使用 `npm` 作为前端包管理工具

### 4. 问题修复

- 修复了后端对 `Lombok` 隐式依赖导致的编译失败
- 调整 `springdoc-openapi` 版本，解决与当前 `Spring Boot 3.3.x` 的兼容问题
- 完成前后端本地启动与基础接口验证

### 5. Git 与 GitHub

- 初始化本地 Git 仓库
- 与远程 `main` 历史合并
- 配置本地 Git 用户名与邮箱
- 生成 GitHub SSH key
- 切换仓库远程地址到 SSH
- 成功推送项目到 GitHub

## 当前项目状态

- 前端项目已可运行
- 后端项目已可打包并启动
- 登录接口可返回演示 token
- 用户信息接口可返回演示用户数据
- 仓库已推送至 GitHub

## 当前仓库地址

- [GitHub 仓库](https://github.com/nannan23449/h5_practice_pro)

## 后续建议

1. 实现真实 JWT 登录鉴权
2. 新增业务模块，例如商品、订单、预约
3. 接入真实数据库查询
4. 增加测试与部署文档

## 补充说明

本次整理后，仓库已经具备继续迭代的基础形态，后续可以直接在当前结构上继续做业务开发。
